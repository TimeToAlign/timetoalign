# TiliaDictStore { #timetoalign.loader.alignment.tilia.TiliaDictStore }

```python
loader.alignment.tilia.TiliaDictStore(data=None, kind_map=None)
```

``DictStore`` subclass with convenience properties for TiLiA timeline types.

Each property returns the concatenation of all ``EventData`` tables
whose TiLiA kind matches the requested type.  If no tables of that
type exist, an empty ``EventData`` is returned.

The type of each entry is tracked via a ``kind_map`` that maps store
keys to short type names (``beat``, ``harmony``, ``hierarchy``,
``marker``, ``pdf``).

## Examples {.doc-section .doc-section-examples}

```python
>>> loader = TiliaJsonLoader()
>>> loader.load("Bruckner5_Scherzo.json")
>>> loader.store.hierarchy  # all hierarchy tables concatenated
>>> loader.store.marker     # all marker tables concatenated
>>> loader.store.beat       # all beat tables concatenated
```

## See Also {.doc-section .doc-section-see-also}

timetoalign.storage.store.DictStore

## Attributes

| Name | Description |
| --- | --- |
| [beat](#timetoalign.loader.alignment.tilia.TiliaDictStore.beat) | All beat timeline tables concatenated. |
| [harmony](#timetoalign.loader.alignment.tilia.TiliaDictStore.harmony) | All harmony timeline tables concatenated. |
| [hierarchy](#timetoalign.loader.alignment.tilia.TiliaDictStore.hierarchy) | All hierarchy timeline tables concatenated. |
| [kind_map](#timetoalign.loader.alignment.tilia.TiliaDictStore.kind_map) | Mapping from store keys to TiLiA type names. |
| [marker](#timetoalign.loader.alignment.tilia.TiliaDictStore.marker) | All marker timeline tables concatenated. |
| [pdf](#timetoalign.loader.alignment.tilia.TiliaDictStore.pdf) | All PDF timeline tables concatenated. |

## Methods

| Name | Description |
| --- | --- |
| [add](#timetoalign.loader.alignment.tilia.TiliaDictStore.add) | Add or replace a named EventData table with optional kind tracking. |

### add { #timetoalign.loader.alignment.tilia.TiliaDictStore.add }

```python
loader.alignment.tilia.TiliaDictStore.add(name, events, kind=None)
```

Add or replace a named EventData table with optional kind tracking.

#### Parameters {.doc-section .doc-section-parameters}

| Name   | Type                                                | Description                                                                                                           | Default    |
|--------|-----------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------|------------|
| name   | [str](`str`)                                        | The name for this data (e.g., ``"HIERARCHY_TIMELINE_0"``).                                                            | _required_ |
| events | [EventData](`timetoalign.storage.events.EventData`) | The EventData to store.                                                                                               | _required_ |
| kind   | [str](`str`) \| None                                | The TiLiA type name (``"beat"``, ``"hierarchy"``, etc.). If ``None``, the kind is inferred from the name if possible. | `None`     |
