# QuartersToFloatingMeasures { #timetoalign.QuartersToFloatingMeasures }

```python
QuartersToFloatingMeasures(
    x_values,
    y_values,
    source_unit=TimeUnit.quarters,
    target_unit=TimeUnit.floating_measures,
    uid=None,
    name=None,
)
```

Map quarters to floating measures.

A floating measure (``fm``) writes a position as
``<ordinal>.<how far into that bar>`` — the convention behind
annotation tables that read ``12.5`` for the middle of the twelfth
bar.  Two properties make it exact rather than approximate:

* **The fractional part is anchored on the NOMINAL bar.** An
  incomplete bar's content sits where it is notated, so a 1/8 pickup
  in 9/8 onsets at ``0.888`` — eight ninths of the way through the
  bar it belongs to — not at ``0.000``.
* **Emission truncates to three decimals.** Three decimals is the
  convention's resolution, and truncating (rather than rounding) is
  what makes ``8/9`` read ``0.888``; it is also the rule behind the
  ``.999`` an interval end shows when a bar is read from the right.

An fm value is a float by definition: the fractional part measures
an uneven discretisation of logical time — bars vary in length — so
it is not a ratio of anything, and its resolution is capped at a
thousandth of a bar.  Converting fm back to quarters is therefore
exact only to that resolution; the inverse interpolates linearly
over the same knots and never tries to reconstruct what truncation
dropped.

## Examples {.doc-section .doc-section-examples}

```python
>>> cmap = QuartersToFloatingMeasures(
...     x_values=[0, 4, 8, 12],
...     y_values=[1.0, 2.0, 3.0, 4.0],
... )
>>> cmap(0.0)    # Start of M1
1.0
>>> cmap(2.0)    # Halfway through M1 (4 quarters/measure)
1.5
>>> cmap(4.0)    # Start of M2
2.0
```

## Methods

| Name | Description |
| --- | --- |
| [from_dict](#timetoalign.QuartersToFloatingMeasures.from_dict) | Deserialize from dictionary. |
| [from_measure_map](#timetoalign.QuartersToFloatingMeasures.from_measure_map) | Build the floating-measure lattice of a measure map. |
| [inverse](#timetoalign.QuartersToFloatingMeasures.inverse) | Return the inverse map (measures -> quarters). |
| [to_dict](#timetoalign.QuartersToFloatingMeasures.to_dict) | Serialize to dictionary. |
| [to_measure_number_map](#timetoalign.QuartersToFloatingMeasures.to_measure_number_map) | Convert to non-interpolating measure number map. |

### from_dict { #timetoalign.QuartersToFloatingMeasures.from_dict }

```python
QuartersToFloatingMeasures.from_dict(data)
```

Deserialize from dictionary.

### from_measure_map { #timetoalign.QuartersToFloatingMeasures.from_measure_map }

```python
QuartersToFloatingMeasures.from_measure_map(measure_map)
```

Build the floating-measure lattice of a measure map.

One knot per measure record, placed at that bar's **virtual
nominal downbeat** — its sounding start minus the offset at which
its content sits inside the notated bar — carrying that bar's
ordinal.  A final knot closes the lattice at the end of the last
bar.  Between two knots the map is linear, so the slope inside a
bar is exactly ``1 / nominal_length``.

Ordinals count the measure records, never the printed labels:
the first record is ``0`` when it is an anacrusis (offset
content, shorter than its nominal bar) and ``1`` otherwise, and
every following record adds one.  Counting runs monotonically
through voltas — ``15a`` and ``15b`` are two bars and get two
consecutive ordinals — and never resets.

#### Parameters {.doc-section .doc-section-parameters}

| Name        | Type                                                       | Description                                                 | Default    |
|-------------|------------------------------------------------------------|-------------------------------------------------------------|------------|
| measure_map | [MeasureMap](`timetoalign.alignment.structure.MeasureMap`) | Measures whose exact starts and lengths define the lattice. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                                                                                 | Description                   |
|--------|--------------------------------------------------------------------------------------|-------------------------------|
|        | [QuartersToFloatingMeasures](`timetoalign.maps.interval.QuartersToFloatingMeasures`) | The fm map for that skeleton. |

#### Raises {.doc-section .doc-section-raises}

| Name   | Type                       | Description                                                                                                                                                 |
|--------|----------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------|
|        | [ValueError](`ValueError`) | If the skeleton has no measures, or if two records share a virtual nominal downbeat (a split bar, whose two halves cannot both anchor the same fm ordinal). |

### inverse { #timetoalign.QuartersToFloatingMeasures.inverse }

```python
QuartersToFloatingMeasures.inverse()
```

Return the inverse map (measures -> quarters).

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                                          | Description                                 |
|--------|-----------------------------------------------|---------------------------------------------|
|        | [TableMap](`timetoalign.maps.table.TableMap`) | A TableMap converting measures to quarters. |

#### Raises {.doc-section .doc-section-raises}

| Name   | Type                                         | Description                                         |
|--------|----------------------------------------------|-----------------------------------------------------|
|        | [NotImplementedError](`NotImplementedError`) | If not invertible (should not happen for this map). |

### to_dict { #timetoalign.QuartersToFloatingMeasures.to_dict }

```python
QuartersToFloatingMeasures.to_dict()
```

Serialize to dictionary.

### to_measure_number_map { #timetoalign.QuartersToFloatingMeasures.to_measure_number_map }

```python
QuartersToFloatingMeasures.to_measure_number_map(mns=None)
```

Convert to non-interpolating measure number map.

#### Parameters {.doc-section .doc-section-parameters}

| Name   | Type                                                  | Description                                               | Default   |
|--------|-------------------------------------------------------|-----------------------------------------------------------|-----------|
| mns    | [Sequence](`typing.Sequence`)\[[str](`str`)\] \| None | MN labels to use. If None, uses floor(y_value) as string. | `None`    |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                                                                           | Description                                       |
|--------|--------------------------------------------------------------------------------|---------------------------------------------------|
|        | [QuartersToMeasureNumber](`timetoalign.maps.interval.QuartersToMeasureNumber`) | QuartersToMeasureNumber with the same boundaries. |
