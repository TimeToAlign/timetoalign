# MatchGraph { #timetoalign.alignment.MatchGraph }

```python
alignment.MatchGraph(
    claims=None,
    *,
    units=None,
    axis=None,
    source=None,
    source_id=None,
)
```

A graph of MatchClaims connecting events across timelines/groups.

The MatchGraph builds a networkx graph where:
- Nodes: (timeline_id, coordinate) tuples
- Edges: synchronous AlignmentAnchors (explicit or implicit)

Only synchronous claims produce graph edges. Non-synchronous claims
(conceptual matches, NOMATCH) are stored in ``_claims`` but do not
create nodes or edges.

Each Hendrix M-box (M1–M15) is a separate MatchGraph. The system
is NOT a global graph; MatchGraphs are created on demand.

## Attributes {.doc-section .doc-section-attributes}

| Name   | Type                                                                      | Description                                                              |
|--------|---------------------------------------------------------------------------|--------------------------------------------------------------------------|
| claims | [list](`list`)\[[MatchClaim](`timetoalign.alignment.claims.MatchClaim`)\] | List of all MatchClaims in this graph (synchronous and non-synchronous). |

## Examples {.doc-section .doc-section-examples}

```python
>>> # Build graph from claims
>>> graph = MatchGraph(claims=[claim1, claim2])
```

```python
>>> # Extend via group membership
>>> extended = graph.extend_to_groups(groups, timeline_to_group)
```

```python
>>> # Get synchronized timestamps
>>> stamps = graph.get_stamps()
```

## Methods

| Name | Description |
| --- | --- |
| [extend_to_groups](#timetoalign.alignment.MatchGraph.extend_to_groups) | Extend anchors to full group timestamps via implicit claims. |
| [filter](#timetoalign.alignment.MatchGraph.filter) | Create filtered view of the graph. |
| [from_dict](#timetoalign.alignment.MatchGraph.from_dict) | Deserialize from dictionary. |
| [get_connected_nodes](#timetoalign.alignment.MatchGraph.get_connected_nodes) | Get all nodes connected to a given node. |
| [get_connected_timelines](#timetoalign.alignment.MatchGraph.get_connected_timelines) | Get all timelines connected to a given timeline. |
| [get_coordinates_for_timeline](#timetoalign.alignment.MatchGraph.get_coordinates_for_timeline) | Get all coordinates for a specific timeline. |
| [get_matchstamp](#timetoalign.alignment.MatchGraph.get_matchstamp) | Get the single MatchStamp for this graph. |
| [get_nodes_for_timeline](#timetoalign.alignment.MatchGraph.get_nodes_for_timeline) | Get all nodes for a specific timeline. |
| [get_stamps](#timetoalign.alignment.MatchGraph.get_stamps) | Get all MatchStamps from the graph. |
| [split_components](#timetoalign.alignment.MatchGraph.split_components) | Split this graph into one MatchGraph per connected component. |
| [to_dict](#timetoalign.alignment.MatchGraph.to_dict) | Serialize to dictionary. |

### extend_to_groups { #timetoalign.alignment.MatchGraph.extend_to_groups }

```python
alignment.MatchGraph.extend_to_groups(
    groups,
    timeline_to_group,
    include_inferred=True,
    *,
    timeline_ids=None,
    id_pattern=None,
    include_domains=None,
    include_units=None,
)
```

Extend anchors to full group timestamps via implicit claims.

For each coordinate in the graph, if it belongs to a Group,
computes the equivalent coordinate for every other member of that
Group and adds an implicit ``MatchClaim`` (case d) plus the
corresponding edge. Filters control which timelines receive
implicit claims.

#### Parameters {.doc-section .doc-section-parameters}

| Name              | Type                                                                  | Description                                | Default    |
|-------------------|-----------------------------------------------------------------------|--------------------------------------------|------------|
| groups            | [dict](`dict`)\[[str](`str`), \'TimelineGroup\'\]                     | Dict of group_id -> TimelineGroup.         | _required_ |
| timeline_to_group | [dict](`dict`)\[[str](`str`), [str](`str`)\]                          | Dict of timeline_id -> group_id.           | _required_ |
| include_inferred  | [bool](`bool`)                                                        | Whether to add inferred edges.             | `True`     |
| timeline_ids      | [set](`set`)\[[str](`str`)\] \| None                                  | Only extend to these timeline IDs.         | `None`     |
| id_pattern        | [str](`str`) \| None                                                  | Regex filter for timeline IDs.             | `None`     |
| include_domains   | [set](`set`)\[[Domain](`timetoalign.core.enums.Domain`)\] \| None     | Only extend to timelines in these domains. | `None`     |
| include_units     | [set](`set`)\[[TimeUnit](`timetoalign.core.enums.TimeUnit`)\] \| None | Only extend to timelines with these units. | `None`     |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type           | Description                                                    |
|--------|----------------|----------------------------------------------------------------|
|        | \'MatchGraph\' | New MatchGraph with extended edges (or self if not extending). |

### filter { #timetoalign.alignment.MatchGraph.filter }

```python
alignment.MatchGraph.filter(
    synchronous_only=False,
    explicit_only=False,
    *,
    timeline_ids=None,
    id_pattern=None,
    include_domains=None,
    include_units=None,
)
```

Create filtered view of the graph.

#### Parameters {.doc-section .doc-section-parameters}

| Name             | Type                                                                  | Description                                | Default   |
|------------------|-----------------------------------------------------------------------|--------------------------------------------|-----------|
| synchronous_only | [bool](`bool`)                                                        | Include only synchronous edges.            | `False`   |
| explicit_only    | [bool](`bool`)                                                        | Include only explicit edges (no inferred). | `False`   |
| timeline_ids     | [set](`set`)\[[str](`str`)\] \| None                                  | Only include these timeline IDs.           | `None`    |
| id_pattern       | [str](`str`) \| None                                                  | Regex filter for timeline IDs.             | `None`    |
| include_domains  | [set](`set`)\[[Domain](`timetoalign.core.enums.Domain`)\] \| None     | Only include timelines in these domains.   | `None`    |
| include_units    | [set](`set`)\[[TimeUnit](`timetoalign.core.enums.TimeUnit`)\] \| None | Only include timelines with these units.   | `None`    |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type           | Description                               |
|--------|----------------|-------------------------------------------|
|        | \'MatchGraph\' | New MatchGraph with filtered edges/nodes. |

### from_dict { #timetoalign.alignment.MatchGraph.from_dict }

```python
alignment.MatchGraph.from_dict(data)
```

Deserialize from dictionary.

### get_connected_nodes { #timetoalign.alignment.MatchGraph.get_connected_nodes }

```python
alignment.MatchGraph.get_connected_nodes(node)
```

Get all nodes connected to a given node.

#### Parameters {.doc-section .doc-section-parameters}

| Name   | Type                                                 | Description                         | Default    |
|--------|------------------------------------------------------|-------------------------------------|------------|
| node   | [GraphNode](`timetoalign.alignment.graph.GraphNode`) | The (timeline_id, coordinate) node. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                                                                   | Description              |
|--------|------------------------------------------------------------------------|--------------------------|
|        | [list](`list`)\[[GraphNode](`timetoalign.alignment.graph.GraphNode`)\] | List of connected nodes. |

### get_connected_timelines { #timetoalign.alignment.MatchGraph.get_connected_timelines }

```python
alignment.MatchGraph.get_connected_timelines(timeline_id)
```

Get all timelines connected to a given timeline.

#### Parameters {.doc-section .doc-section-parameters}

| Name        | Type         | Description            | Default    |
|-------------|--------------|------------------------|------------|
| timeline_id | [str](`str`) | The timeline to check. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                         | Description                    |
|--------|------------------------------|--------------------------------|
|        | [set](`set`)\[[str](`str`)\] | Set of connected timeline IDs. |

### get_coordinates_for_timeline { #timetoalign.alignment.MatchGraph.get_coordinates_for_timeline }

```python
alignment.MatchGraph.get_coordinates_for_timeline(timeline_id)
```

Get all coordinates for a specific timeline.

#### Parameters {.doc-section .doc-section-parameters}

| Name        | Type         | Description                          | Default    |
|-------------|--------------|--------------------------------------|------------|
| timeline_id | [str](`str`) | The timeline to get coordinates for. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                               | Description                  |
|--------|------------------------------------|------------------------------|
|        | [list](`list`)\[[float](`float`)\] | List of coordinates, sorted. |

### get_matchstamp { #timetoalign.alignment.MatchGraph.get_matchstamp }

```python
alignment.MatchGraph.get_matchstamp()
```

Get the single MatchStamp for this graph.

One MatchGraph = one MatchStamp. The MatchStamp is the union of
all coordinates reachable through the graph's edges.

If the graph contains multiple disconnected components, this
method raises ``ValueError`` -- each component should be its own
MatchGraph. Use ``split_components()`` to separate them first, or
use the multi-component ``get_stamps()`` method.

#### Returns {.doc-section .doc-section-returns}

| Name   | Type           | Description                                            |
|--------|----------------|--------------------------------------------------------|
|        | \'MatchStamp\' | Single MatchStamp spanning all timelines in the graph. |

#### Raises {.doc-section .doc-section-raises}

| Name   | Type                       | Description                                        |
|--------|----------------------------|----------------------------------------------------|
|        | [ValueError](`ValueError`) | If the graph has multiple disconnected components. |
|        | [ValueError](`ValueError`) | If the graph has no synchronous claims (no nodes). |

#### See Also {.doc-section .doc-section-see-also}

`split_components`: Split a multi-component graph into separate
    MatchGraph objects.
`get_stamps`: Multi-component method returning one stamp per component.

### get_nodes_for_timeline { #timetoalign.alignment.MatchGraph.get_nodes_for_timeline }

```python
alignment.MatchGraph.get_nodes_for_timeline(timeline_id)
```

Get all nodes for a specific timeline.

#### Parameters {.doc-section .doc-section-parameters}

| Name        | Type         | Description                    | Default    |
|-------------|--------------|--------------------------------|------------|
| timeline_id | [str](`str`) | The timeline to get nodes for. | _required_ |

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                                                                   | Description                              |
|--------|------------------------------------------------------------------------|------------------------------------------|
|        | [list](`list`)\[[GraphNode](`timetoalign.alignment.graph.GraphNode`)\] | List of (timeline_id, coordinate) nodes. |

### get_stamps { #timetoalign.alignment.MatchGraph.get_stamps }

```python
alignment.MatchGraph.get_stamps()
```

Get all MatchStamps from the graph.

Returns one MatchStamp per connected component, each containing
all coordinates reachable from that component.

Returns:
    List of MatchStamps, one per connected component.

### split_components { #timetoalign.alignment.MatchGraph.split_components }

```python
alignment.MatchGraph.split_components()
```

Split this graph into one MatchGraph per connected component.

Each returned MatchGraph represents a single connected component
and can be queried with ``get_matchstamp()``.

#### Returns {.doc-section .doc-section-returns}

| Name   | Type                             | Description                                              |
|--------|----------------------------------|----------------------------------------------------------|
|        | [list](`list`)\[\'MatchGraph\'\] | List of MatchGraph objects, one per connected component. |
|        | [list](`list`)\[\'MatchGraph\'\] | Empty list if the graph has no synchronous claims.       |

### to_dict { #timetoalign.alignment.MatchGraph.to_dict }

```python
alignment.MatchGraph.to_dict()
```

Serialize to dictionary.

Note: This serializes the claims, not the full graph.
The graph can be rebuilt from claims.
